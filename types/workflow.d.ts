/// <reference types="eve/workflow-modules" />
// The `workflow` and `workflow/api` specifiers are resolved by eve from its
// vendored SDK; the types are ambient. Referenced from a file rather than
// through compilerOptions.types, which would stop every other @types package
// from being included automatically.
